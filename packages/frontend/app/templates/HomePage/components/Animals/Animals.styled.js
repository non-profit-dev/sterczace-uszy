import styled from "@emotion/styled"

export const Grid = styled.div`
  display: grid;
  grid-auto-flow: column;
  gap: 40px;
  margin-top: 20px;
  margin-bottom: 40px;

  img {
    max-height: 340px;
  }
`

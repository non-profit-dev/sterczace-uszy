import { Meta, IconGallery, IconItem } from "@storybook/addon-docs"

import { illustrations } from "design-system/components/illustration"

<Meta title="Illustrations" />

# Illustrations gallery

A list of all available illustrations in the project.

<IconGallery>
  {Object.entries(illustrations).map((illustration, i) => (
    <IconItem key={i} name={illustration[0]}>
      {illustration[1]()}
    </IconItem>
  ))}
</IconGallery>

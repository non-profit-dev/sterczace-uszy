import { Meta, Canvas, ArgTypes } from "@storybook/blocks"

import * as Stories from "./Card.stories"

<Meta of={Stories} />

# Card

It's a flexible component used to group and display content in a concise format.

`import Card from "design-system/patterns/card"`

<Canvas of={Stories.Overview} />

<ArgTypes />

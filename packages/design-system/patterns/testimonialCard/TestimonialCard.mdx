import { Meta, Canvas, ArgTypes } from "@storybook/blocks"

import * as Stories from "./TestimonialCard.stories"

<Meta of={Stories} />

# TestimonialCard

It's a card component but used to display testimonials.

`import TestimonialCard from "design-system/components/testimonialCard"`

<Canvas of={Stories.Controls} />

<ArgTypes />

import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs"

import ScrollButton from "design-system/components/scrollButton"

<Meta title="Components/ScrollButton" component={ScrollButton} />

# ScrollButton

Allows users to scroll to a particular page section.

`import ScrollButton from "design-system/components/scrollButton"`

<Canvas>
  <Story
    name="Overview"
    args={{
      sectionIdToScroll: "contact-section",
      ariaLabel: "Scroll to example",
    }}
  >
    {Template.bind()}
  </Story>
</Canvas>

<ArgsTable />

export const Template = (args) => <ScrollButton {...args} />

import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs"

import ListItem from "design-system/components/listItem"

<Meta title="Components/ListItem" component={ListItem} />

# ListItem

Displays a list item element, as a `li` tag.

`import ListItem from "design-system/components/listItem"`

<Canvas>
  <Story
    name="With icon"
    args={{
      children: "umożliwiamy adopcję za granicę",
      variant: "primary",
      iconName: "done",
    }}
  >
    {Template.bind()}
  </Story>
  <Story
    name="Without icon"
    args={{
      children: "umożliwiamy adopcję za granicę",
      variant: "gray",
    }}
  >
    {Template.bind()}
  </Story>
</Canvas>

<ArgsTable />

export const Template = (args) => <ListItem {...args} />

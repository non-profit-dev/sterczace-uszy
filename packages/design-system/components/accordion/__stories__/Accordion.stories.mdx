import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs"
import { useTheme } from "@emotion/react"

import Accordion from "design-system/components/accordion"
import Button from "design-system/components/button"
import Typography from "design-system/components/typography"
import List from "design-system/components/list"
import ListItem from "design-system/components/listItem"

<Meta title="Components/Accordion" component={Accordion} />

# Accordion

<Canvas>
  <Story
    name="Overview - black"
    args={{
      color: "black",
      activeColor: "black",
      text: "Fundacja",
    }}
  >
    {TemplateBlack.bind()}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Overview - black with primary active"
    args={{
      text: "Fundacja",
      color: "black",
      activeColor: "primary",
    }}
  >
    {TemplateBlack.bind()}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Overview - white with primary active"
    args={{
      text: "Fundacja",
      color: "white",
      activeColor: "primary",
    }}
  >
    {TemplateWhitePrimary.bind()}
  </Story>
</Canvas>

<ArgsTable />

export const TemplateBlack = (args) => {
  const theme = useTheme()
  return (
    <Accordion {...args}>
      <Typography color={theme.colors.grey[600]} variant="bodySmall">
        Tu wpisz odpowiedź na pytanie
      </Typography>
      <Button
        text="Button"
        variant="text"
        color="primary"
        size="small"
        iconEnd="arrowRight"
      />
    </Accordion>
  )
}

export const TemplateWhitePrimary = (args) => (
  <Accordion {...args}>
    <List gap={4}>
      <ListItem>
        <Button
          variant="text"
          color="white"
          size="small"
          text="O nas"
          href="#"
        />
      </ListItem>
      <ListItem>
        <Button
          variant="text"
          color="white"
          size="small"
          text="Aktualności"
          href="#"
        />
      </ListItem>
      <ListItem>
        <Button
          variant="text"
          color="white"
          size="small"
          text="Opinie"
          href="#"
        />
      </ListItem>
      <ListItem>
        <Button
          variant="text"
          color="white"
          size="small"
          text="Partnerzy"
          href="#"
        />
      </ListItem>
    </List>
  </Accordion>
)

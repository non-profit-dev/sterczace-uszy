import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs"

import Count from "design-system/components/count"

<Meta title="Components/Count" component={Count} />

# Count

Displays a number value in a custom format and style.

`import Count from "design-system/components/count"`

<Canvas>
  <Story
    name="Overview"
    args={{
      count: 3,
      size: "small",
    }}
  >
    {Template.bind()}
  </Story>
</Canvas>

<ArgsTable />

export const Template = (args) => <Count {...args} />

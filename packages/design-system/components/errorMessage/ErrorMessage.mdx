import { Meta, Canvas, ArgTypes } from "@storybook/blocks"

import * as Stories from "./ErrorMessage.stories"

<Meta of={Stories} />

# ErrorMessage

Displays an error message in input, text and checkbox components if an error occurred.

`import ErrorMessage from "design-system/components/errorMessage"`

<Canvas of={Stories.Controls} />

<ArgTypes />

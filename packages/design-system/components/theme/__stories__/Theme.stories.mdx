import { Meta, ColorPalette, ColorItem } from "@storybook/addon-docs"

import theme from "../theme"

<Meta
  title="Theme"
  parameters={{
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

# Colors

<ColorPalette>
  {Object.entries(theme.colors).map((palette, i) => (
    <div key={i}>
      <ColorItem key={palette[0]} title={palette[0]} colors={palette[1]} />
    </div>
  ))}
</ColorPalette>

import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs"

import IconButton from "design-system/components/iconButton"

<Meta title="Components/IconButton" component={IconButton} />

# IconButton

Allows users to take actions on a website based only on the visual element (icon).

`import IconButton from "design-system/components/iconButton"`

<Canvas>
  <Story
    name="Overview - link variant"
    args={{
      name: "mail",
      size: "large",
      href: "https://google.com",
      ariaLabel: "Example link",
      title: "Example title",
    }}
  >
    {Template.bind()}
  </Story>
</Canvas>

<ArgsTable />

export const Template = (args) => <IconButton {...args} />

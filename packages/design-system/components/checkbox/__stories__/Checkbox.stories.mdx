import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs"

import Checkbox from "design-system/components/checkbox"

<Meta title="Components/Checkbox" component={Checkbox} />

# Checkbox

Allows users to select single values for submission in a form.

`import Checkbox from "design-system/components/checkbox"`

<Canvas>
  <Story
    name="Overview"
    args={{
      label: "Text",
    }}
  >
    {Template.bind()}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Overview - disabled"
    args={{
      label: "Text",
      disabled: true,
    }}
  >
    {Template.bind()}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Overview - error"
    args={{
      label: "Text",
      state: "error",
    }}
  >
    {Template.bind()}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Overview - active"
    args={{
      label: "Text",
      state: "acrive",
    }}
  >
    {Template.bind()}
  </Story>
</Canvas>

<ArgsTable />

export const Template = (args) => (
  <div style={{ maxWidth: "270px" }}>
    <Checkbox {...args} />
  </div>
)

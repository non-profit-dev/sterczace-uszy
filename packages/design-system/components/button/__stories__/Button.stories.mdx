import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs"

import Button from "../Button"
import Icon from "../../icon/Icon"

<Meta title="Components/Button" component={Button} />

# Button

Button can be a regular button, a link or a navigation link.

<Canvas>
  <Story
    name="Overview - link variant"
    args={{
      text: "Przykładowy tekst",
      href: "https://google.com",
      variant: "fill",
      size: "medium",
    }}
  >
    {Template.bind()}
  </Story>
  <Story
    name="Overview - button variant"
    args={{
      text: "Przykładowy tekst",
      iconEnd: <ArrowDownIcon />,
      variant: "fill",
      size: "medium",
    }}
  >
    {Template.bind()}
  </Story>
  <Story
    name="Overview - button variant2"
    args={{
      text: "Przykładowy tekst",
      iconEnd: <Icon name="arrowDown" />,
      variant: "fill",
      size: "medium",
    }}
  >
    {Template.bind()}
  </Story>
</Canvas>

<ArgsTable />

export const Template = (args) => <Button {...args} />

export const ArrowDownIcon = () => (
  <svg
    width="28"
    height="16"
    viewBox="0 0 14 8"
    fill="none"
    xmlns="http://www.w3.org/2000/svg"
  >
    <path
      d="M1 1L7 7L13 1"
      stroke="currentColor"
      strokeWidth="2.2"
      strokeLinecap="round"
      strokeLinejoin="round"
    />
  </svg>
)

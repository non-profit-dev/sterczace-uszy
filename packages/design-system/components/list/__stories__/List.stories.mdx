import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs"

import List from "../List"
import ListItem from "../../listItem/ListItem"

<Meta title="Components/List" component={List} />

# List

<Canvas>
  <Story
    name="Overview"
    args={{
      gap: "8px",
      children: (
        <>
          <ListItem children={<>Lista element 1</>} iconName="done" />
          <ListItem
            children={<>Lista element 2</>}
            iconName="done"
            variant="gray"
          />
          <ListItem children={<>Lista element 3</>} iconName="done" />
        </>
      ),
    }}
  >
    {Template.bind()}
  </Story>
</Canvas>

<ArgsTable />

export const Template = (args) => <List {...args} />

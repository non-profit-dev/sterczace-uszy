import { Meta, IconGallery, IconItem } from "@storybook/addon-docs"

import { icons } from "../../components/icon/Icon.jsx"

<Meta title="Icons" />

# Icons gallery

A list of all available icons in the project.

<IconGallery>
  {Object.entries(icons).map((icon, i) => (
    <IconItem key={i} name={icon[0]}>
      {icon[1]()}
    </IconItem>
  ))}
</IconGallery>

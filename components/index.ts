export { Hero } from "./hero"
export { PartnerCard } from "./partner-card"
export { PetCard } from "./pet-card"
// Note: Section should be imported directly from "@/components/section"
// because compound components (Section.Header etc.) don't work with barrel re-exports
